 MakeScript←{
⍝ ⍵ ←→ Options JSON Object
⍝ ← ←→ (Error number)(Error message)

     script←GenerateScript ⍵
     b←script ⎕NPUT ⍵.outpath 1
     0('Saved: ',⍵.outpath,' (',(⍕b),' bytes)')

⍝ TODO
⍝ ! → {!}
⍝ \ → VKDC
⍝ Ctrl → <^> :: c v s shifted
⍝ allow specify left or right
⍝ Alt <! <!> >!
⍝ Win
 }
